import type { TextMatch } from "../shared/types";

/**
 * TODO: Implementasi Rabin-Karp from scratch.
 *
 * Bonus specification:
 * - Digunakan sebagai algoritma alternatif.
 * - Menggunakan rolling hash.
 * - Tetap perlu verifikasi karakter ketika hash cocok.
 * - Tidak boleh bergantung pada built-in string search.
 */
export function rabinKarpSearch(
  _text: string,
  _keyword: string,
  _targetId: number,
): TextMatch[] {
  return [];
}
